# v0.3.0

面向完全不熟悉浏览器扩展安装流程的开箱即用版本。

- 新增 `使用说明.html`，下载后离线打开即可看到安装和使用步骤。
- 新增发布打包脚本 `scripts/package-release.ps1`，生成的 zip 解压后是单一中文文件夹，更适合直接拖入扩展管理页。
- README 改成更直接的“小白安装”流程，并提醒下载 release 附件而不是 GitHub 自动生成的源码包。
- 扩展按钮和提示文案调整为更清晰的中文，例如“使用当前页”“开始下载回放”。
- 错误提示补充下一步操作，首次使用时更容易判断是链接、登录还是权限问题。

仅适用于当前钉钉账号已经有权限访问的回放内容。
